export class Workarea {
  width: string;
  height: string;
  border: string;
  text: string;

  constructor(width: string, height: string, border: string, text: string) {
    this.width = width;
    this.height = height;
    this.border = border;
    this.text = text;
  }
}